# INVALID: schema pin missing the @YYYY.N form (#SchemaRef). The version keys
# travel onto DepositConstituted and never move (authoring §6), so a malformed
# pin must be rejected at depth 1.
variant_id: dpz_pt_12m_bad_schema_ref
schema: term_deposit-2026.1
pack: pt.2026.1
term_days: 365
day_count: pt.act_360
currency: EUR
interest_variant: AT_MATURITY
rate:
  flat: { rate_ref: { sheet: live, role_selector: deposit_origin } }
early_termination:
  flat: { up_to_days: null, penalty_basis_points: 10000, basis: ACCRUED_INTEREST }
auto_renewal_policy: NONE
principal_bounds: { min_cents: 50000 }
