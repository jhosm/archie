# INVALID: max_cents below min_cents — an inverted risk corridor. Regression
# guard for the #PrincipalBounds cross-field guard (subtle: the `if max_cents
# != _|_` presence test fires only when max_cents is concrete).
variant_id: dpz_pt_12m_inverted_corridor
schema: term_deposit@2026.1
pack: pt.2026.1
term_days: 365
day_count: pt.act_360
currency: EUR
interest_variant: AT_MATURITY
rate:
  flat: { rate_ref: { sheet: live, role_selector: deposit_origin } }
early_termination:
  flat: { up_to_days: null, penalty_basis_points: 10000, basis: ACCRUED_INTEREST }
auto_renewal_policy: NONE
principal_bounds: { min_cents: 100000, max_cents: 50000 }
