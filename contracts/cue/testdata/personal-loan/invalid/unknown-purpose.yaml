# REJECT: `purpose` carries a value outside the engine-owned closed taxonomy
# (#Purpose). `vacation` is not a recognised purpose bucket, so it fails depth 1
# — the no-DSL-escape-hatch guarantee (ADR-PC-006 Decision).
variant_id: cp_pt_vacation_12m
schema: personal_loan@2026.1
pack: pt.2026.1
currency: EUR
term_months: 12
purpose: vacation
rate:
  fixed:
    rate_ref: { sheet: live, role_selector: standard }
early_repayment:
  commission_basis_points: 25
principal_bounds:
  min_cents: 100000
