<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>Babelstone.Pii</RootNamespace>
    <AssemblyName>Babelstone.Pii</AssemblyName>
  </PropertyGroup>

  <!-- No project references. Babelstone.Pii isolates the deliberate exception to the
       hand-rolled-core posture (ADR-PC-004 §Decision): the OpenBao client lives here
       and nowhere else, so the exception is visible in the project graph (skeleton
       §2.1/§3). Babelstone.Engine references this; this references no engine code.
       OpenBao is reached over its transit HTTP API via the BCL HttpClient. -->
</Project>
