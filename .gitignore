
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Beads / Dolt files (added by bd init)
.beads/proxieddb/

# macOS metadata
.DS_Store

# Build output — .NET (engine, orchestrator, acl, notification)
bin/
obj/

# Build output — Go (pack-validate)
/pack-validate/pack-validate

# Build output — pack OCI artefact layouts (packs/pack.sh build)
/.pack-build/

# Build output — Python (mcp-server)
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# Claude Code transients (agent worktrees, scheduler lock)
.claude/worktrees/
.claude/scheduled_tasks.lock

# Editor / backup artefacts — never commit (e.g. kustomization.yaml.bak)
*.bak

# Walking-skeleton demo runtime (scripts/demo-mcp.sh): logs + pidfiles
/.demo-mcp/

# Build output — DocFX site (ADR-PC-026): generated API metadata + rendered site
/docfx/api/
/docfx/_site/
# DocFX structured warning log consumed by the §P6 link-gate (CI-only artifact)
/docfx/build.log.json
