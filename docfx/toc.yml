# Top navbar for the API-reference site (ADR-PC-026 §P3). This site publishes ONLY
# the C# API reference; the hand-authored corpus is read on GitHub (version-correct
# relative links), not stitched here — so the navbar is a one-entry door plus an
# outbound link to the repo docs. The outbound link pins `main` deliberately: this
# Pages artifact is built and deployed only from `main`, so its chrome reflects main.
- name: API Reference
  href: api/
- name: Docs on GitHub
  href: https://github.com/jhosm/babelstone/tree/main/docs
